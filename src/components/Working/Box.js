import React from 'react';
import './Box.scss'
import { useStaticQuery, graphql } from "gatsby"
import Img from "gatsby-image"

const Box = ({idx}) => {

    return (
        <div className="Box">
            
        </div>
    );
};

export default Box;